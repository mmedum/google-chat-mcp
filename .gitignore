# Go build artefacts
/google-chat-mcp
# `go build ./scripts/gates` leaves this at the root. It is a 10 MB
# artefact and it was committed once by exactly that accident.
/gates
/dist/
/schemas.json
cov.out
coverage.out

# Never commit an OAuth client
client_secret*.json
*.client_secret.json

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local tooling state (Claude Code + Entire). Kept even when the tool is
# not installed: this repository is public, and Entire's checkpoints are
# session transcripts. The cost of the line is nothing; the cost of its
# absence is a push nobody meant to make.
.claude/
.entire/
